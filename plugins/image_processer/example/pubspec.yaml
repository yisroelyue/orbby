name: orbby_plugin_image_processer_example
description: "Demonstrates how to use the orbby_plugin_image_processer plugin."
publish_to: 'none'

environment:
  sdk: ^3.12.1

dependencies:
  flutter:
    sdk: flutter

  orbby_plugin_image_processer:
    path: ../

  cupertino_icons: ^1.0.8
  file_picker: ^8.0.0
  path: ^1.9.0
  image: ^4.5.3

dev_dependencies:
  integration_test:
    sdk: flutter
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
